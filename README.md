This webapp gets data from the Yahoo Finance API and uses the Facebook Prophet Library to predict future prices of the stocks.

Libraries required to run: streamlit, prophet, yfinance, plotly, and Anaconda
